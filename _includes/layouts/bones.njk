<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>{{ title or metatitle }} · Jan Früchtl</title>
  <meta name="description" content="{{ renderData.description or description or metadata.description }}">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <meta name="twitter:card" content="summary" />
  <meta name="twitter:site" content="@_coolcut" />
  <meta name="twitter:creator" content="@_coolcut" />

  <meta property="og:title" content="{{ renderData.title or title or metadata.title }}" />
  <meta property="og:description" content="{{ renderData.description or description or metadata.description }}" />
  <meta name="twitter:title" content="{{ renderData.title or title or metadata.title }}">
  <meta name="twitter:description" content="{{ renderData.description or description or metadata.description }}">

  <link rel="alternate" type="application/rss+xml" title="RSS Feed for 2020.fruechtl.me" href="{{ '/feed.xml' | url }}" />

  <style>{% include "css/main.css" %}</style>

  {% if layout == "layouts/post.njk" or bodyClass == "typography-test" %}
  <link rel="stylesheet" href="{{ '/node_modules/lite-youtube-embed/src/lite-yt-embed.css' | url }}">
  {% endif %}
</head>
<body class="{{ bodyClass }}">
  <div class="redesign-notice">This site currently gets redesigned in the open → <a href="/journal/hi-there/">Learn more</a></div>
  {% include "components/site-header.njk" %}
  {{ content | safe }}
  {% include "components/site-footer.njk" %}
</body>
</html>
